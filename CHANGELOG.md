# 版本记录

每一条都对应一个真机上出现过的具体问题。

---

## v1.40(09-24 发布, 一键更新)

- **GPU 版推演换成新网络 netC300**(netB300 接着训 300 轮, 自博弈 PPO): 网络多了 3 项输入, 全从打分模型的两两配合表算
  (随插件发 data/pairP.bin, 与训练端同一份): **我的潜在配合**(这件和池里还没人拿、我还拿得到的东西最强能配多少)、
  **对面已有的配合**(这件和对手已拿到手的东西配多少)、**对面的潜在配合**。以前网络只看"这件对我立刻涨多少",
  所以"单件弱、配在一起强"的组合件(暗影波/复制之墙、叠油/火猫)要么看不懂, 要么推演里以为没人抢、拿弱配件赌强件回来。
  · 纯直觉对 netB300: 胜率 52.9%、赢 64% 的局(含蝙蝠/莉娜/撼地者/昆卡的池子略高);
  · 插件推演对推演(10 人全用各自网络, 组合池/普通池各 120 局): 打平(50.1% / 50.4%) —— 推演本身已经纠正了旧网络大部分错误;
  · 用户指出的怪推荐: 09-24 飞刀局第 5 手暗影波(旧: 标准答案第 3)→ 第 9, 首推复制之墙/巨牙海民; 蝙蝠局"先拿烈火罩赌叠油回来"(旧: 第 1)→ 第 7。
  · 待实战观察: 新网络更少"断人"(对面已拿暗影波时不优先断复制之墙; 对面已拿叠油时不优先断火猫, 推演里指望队友后面去拿)。
  网络档/预估(CPU)仍用 netB300。新输入的插件实现与训练端逐位一致(test/ext_fast.js, 88 局面最大差 3e-8)。
- 耗时: 新输入要在 CPU 上算, 4090 上后台筛选约 3~4 秒、更新约 2 秒(旧网络约 1~1.5 秒); 决赛结果 0.4 秒先显示, 不受影响。

---

## v1.39.1(09-24 发布, 一键更新)

- **开了直播模式后选技中途整个插件卡死, 只能任务管理器**(09-23 飞刀两次, 16:03 / 22:49): 直播模式下常驻视频流抓的是 Dota 2 窗口,
  两局之间窗口流常被系统结束("视频流被结束"); 插件退到逐帧截屏, 而这条路在直播模式下**每 0.25 秒给所有窗口各出一张全尺寸缩略图**再挑 Dota,
  且再也回不到视频流(直播模式的重连轮询在 capMode==="window" 时直接跳过, 逐帧路正好把它设成 window)。两次都是退到逐帧后 5~8 分钟:
  截屏 5ms→40ms、画面频繁"整块被盖住", 然后主进程卡死(日志/托盘/F6/悬浮窗全不响应)。服务器上只有这一个号开过直播模式、也只有它出现过流断回退。现在:
  · 视频流断了每 10 秒(失败翻倍, 最多 60 秒)自动重连, 恢复后直播模式自动回来;
  · 逐帧退路只抓屏幕(一个源), 不再枚举所有窗口; 这段时间悬浮窗暂时恢复防捕获;
  · 截屏调用 3 秒不返回就放弃这一帧并记日志。
- **推荐理由**: GPU 首推不是网络首选时, 顶栏下多一行「💡 推荐 X 而不是 Y: 胜率高 N 点; 若改拿 Y, X 在你下次轮到前 p% 会被 对面R1 拿走」。
  GPU 线程上假设"我改拿网络首选", 推到下一次轮到我(128 局), 看首推那件落到谁手里(4090 约 0.2~0.4 秒, "更新"算完后跑一次)。
  用户 09-24: 断人型推荐(影魔 > 余震、血魔 > 潮汐使者)看着怪, 写清原因让人自己判断对面真人会不会拿。

---

## v1.39(09-24 发布, 一键更新)

- **GPU 版推荐像抽签(冷门技能排第一)**: 推演时每一局的随机数流 = 打散(种子 × 2654435761 + 局号)。
  插件的种子是英雄池哈希(1~5 亿), 乘出来约 10¹⁷, 超过浮点能精确表示的整数(2⁵³≈9×10¹⁵), "+ 局号"被舍入掉 →
  一批里几乎所有推演局用同一串随机数: 决赛 160 局平均只有 5.8 串不同的(58% 只有 1~2 串), 筛选约 1000 局中位 2 串。
  每个候选名义上推 32 局, 实际只推了一两种剧本, 名次靠运气。09-24 00:20 那局 R2 被推荐陵卫斗篷(正确推演: 授予力量 55.9 vs 陵卫斗篷 41.2,
  不拿授予力量 98% 被下一手对面拿走)、R3 被推荐冰霜新星都是它。所有人日志里推演"不同局面"只有 3~9%(正常 75~83%)。
  v1.38 引入(新推演路径), v1.37 及以前的老路径没有这个问题; 09-23 验证和回归都用小种子(5/6/7)所以没发现。
  改成 32 位整数乘法再加局号。GPU 机实测 R2 那一手 6 个大种子: 修复版首推全是授予力量(55.5~58.5), 发布版在棒击大地/大圣归来/授予力量间乱跳。
  新回归 test/simrng_seed.js(v1.38 必失败)。
- **悬浮窗卡在屏幕上关不掉, 只能任务管理器**(09-23 飞刀反馈): 覆盖层常驻显示、置顶、鼠标穿透, "隐藏"只是让它画空白;
  它的页面一旦卡住, 最后一帧就一直钉在最上层, F6 也清不掉。以前主进程对覆盖层卡死/崩溃没有任何监听。现在:
  · 覆盖层"无响应"超过 3 秒 / 渲染进程没了 / 10 秒没收到它报平安(每 2 秒一次) → 自动销毁重建(10 分钟内最多 5 次), 并上传日志;
  · 托盘新增「悬浮窗卡住了? 点这里重建」;
  · 主进程每 30 秒记一行心跳(识别线程多久没消息、覆盖层多久没报平安、2 秒画了几帧), 下次卡住能看出是谁先停的。
- **卡死那次的日志传不上来**: 被任务管理器结束的会话, 日志一直留在用户电脑上(重启后只传新会话)。现在启动 30 秒后,
  上一次会话日志末尾没有"退出"就补传那份日志 + 同时段轨迹(why=prevcrash, 每份只补一次)。回归 test/prev_unclean.js。
- **日志**: 我刚落子、高亮还停在我身上时, 回合小结不再多记一行假的"整个回合没显示GPU结果"(只影响日志, 不影响推荐)。

---

## v1.38(09-23 发布, 一键更新)

- **轮到我了, GPU 版却整个回合不启动**: 09-19~22 所有人上传的日志里 350 个回合有 35 个如此(例: 09-22 21:26 L4 拿弹无虚发)。
  已选手数把"棋盘变暗但没配上面板"的格子也算了进去, 多算 1 手就触发 v1.35 的"高亮慢一拍"规则, 被当成我已经选完、
  改去预估我下一轮(预估只跑网络档)。现在高亮在我身上时, 我选没选完以**我面板上配对确认的件数**为准。新回归 test/me_not_picked.js(旧版必失败)。
- **GPU 版新算法**(09-23 三局 150 个决策点验证, 耗时按 3060 估):
  · 别人选的时候, 后台从当前真实局面把我下一手的**全部候选**都推演一遍(普通卡: 全部×8 → 前 6×32; 快卡: 全部×16 → 前 10×32),
    每有人落子就重算。以前只推演网络挑的前 8 个, 有 16% 的决策最优那件根本没进候选。
  · 轮到我时对表上前 5 名用准确局面各推演 32 局(3060 最慢约 1.2 秒), 马上显示; 之后再用准确局面把全部候选重算一遍, 更新一次
    (设置里"GPU版更新"开关, 默认开)。平均比"全部候选算足"少: 旧版 0.32pp → 不更新 0.17pp → 更新后 0.09pp。
  · 档位按启动时测出的显卡速度自动定(每次 128 局 ≤8ms 算快卡), 写进日志"显卡测速"。
- **半精度网络**(data/netB300_fp16.onnx): 启动时同一块显卡上单/半精度各测一次, 半精度快 10% 以上才用, 建不起来就留单精度(日志"组合版预热"里写明选了哪个)。
  09-23 实测(4090 CUDA): 每次 128 局 1.8ms → 1.2ms; 三局约 700 个真实局面出手概率最大差 0.84pp、首选一致 99.6%+, 新流程端到端损失与单精度无差别。
  4090 上新流程耗时: 开局后台筛选 1.3~1.7s / 决赛 0.16~0.26s / 更新 0.9~1.1s, 中后期都在 0.5s 以内。
- **GPU 版提速(结果与原来逐位相同)**: 同一局面只过一次网络; 后台表里"我前面那几手和真实发生的一样"的推演局直接复用
  (前一个人选的正是网络预料的那一手时, 决赛几乎不用再算); 显卡算一块时 CPU 同时编码下一块、抽样上一块。
- **GPU 结果不再等 CPU 上的现役推演**: 以前 GPU 算完还要等现役推演算完才一起显示(用户机上多等 2~3 秒, 忙的机器整回合出不来),
  现在 GPU 结果一出来就显示, 现役算完只补上"我方"那一列。
- **日志**: 每个我自己的回合写一行"回合小结"(轮到我/落子时刻, 后台筛选/决赛/更新各在第几秒、各用多久, 没显示 GPU 的原因);
  每次推荐把全部候选的分数都写出来(以前只写前三)。
- **日志上传**: 截图转 JPEG(8MB → 约 1MB), 切成 512KB 分块上传、每块失败重试(09-22 晚整局日志包几乎全部上传超时)。
  服务器端 09-23 已更新, 旧版插件照常能传。

---

## v1.37(09-20 发布, 一键更新)

- **选技中途切回大厅打字, 回来后推荐全乱 / GPU 版报错**: 09-19 23:05 那局, 用户切到 Dota 大厅聊了 4 秒,
  棋盘那 60 格的位置上显示的是大厅背景和黑聊天框 —— 在插件眼里就是"一大片格子同时变黑", 而变黑=被选走。
  整块遮挡守卫只顶 8 帧(约 2 秒)就放行(旧注释认为"超过 2 秒就是真的切后台了"), 再 5 帧全部认成被选走,
  暗满 12 帧后永久锁定不撤 → 已选走一瞬间从 24 件跳到 51 件, 其中 24 件配不上是谁拿的。
  这些东西从池子里扣掉却不占任何人的槽位, 于是引擎算出"大家还要选 17 手"而"池子里只剩 11 样", 差 7 样,
  推演必然走到某座位没得选 —— GPU 版当场报错 8 次(v1.36 起不崩了, 但账还是错的, 推荐质量照样打折)。
  现在**画面不是选技棋盘的那几帧, 整帧当看不清, 一个字都不记**。判据用相对基准而不是写死的亮度:
  选技画面里总有没被选走的图标是高光的, 拿"本帧最亮的一格"和"这一局见过的最亮"比, 低于 72% 就判画面不对。
  实测(两人同一局的真机轨迹): 正常选技帧 = 100%, 切大厅 11 秒 = 64~68%, 选完转场 = 31~65%, 偶发单帧闪动 = 78%。
  回放那局真机轨迹: 配不上归属的 24 件 → 1 件, 账目对不上的帧 184/339 → 0/339, 整局 50 手全部正确归属;
  另一个人同一局的轨迹(他没切大厅)也从 10 件 → 2 件, 没有变差。新回归 test/offscreen.js(旧版必失败)。
- **切出去再回来被当成"换了一局", 整局记账清零**: 判"还是不是这一局"只看最大单格亮度差(阈值 25), 可是离开期间
  队友选走一件, 那一格就掉几十(09-19 那次实测 88.7) → 直接重锁。同一局的棋盘只会越选越暗, 换一局才会大片变亮,
  所以加一条"没有大片变亮 = 还是这一局"(实测: 同一局离开再回来变亮 0 格, 开局到局末变亮 0 格, 换一局变亮 59 格)。

---

## v1.36(09-20 发布, 一键更新)

- **看五个人(团队模式)时 GPU 版报错、一直退回网络档**: GPU 版推演走子时, 如果轮到的那个座位在池子里
  已经没有任何能拿的东西(09-19 23:05 那局: 英雄池只有 12 个、已被认掉 11 个, 而右 5 还没有英雄),
  抽样返回"没得选", 旧版直接拿 -1 去落子就崩了。现役 AI 那条路一直有保护, GPU 版这条没有。
  现在这种手**空过**(不能中断, 否则同一批推演的步数会错开)。09-19 日志里 8 次, 都在团队模式算队友时;
  那一局 GPU 版跑 23 次崩 8 次, 表现为「出错, 用网络档」、⚡GPU版 结果一直不出来。
  日志新增「空过 N 手(有人没得可选)」计数。复现/回归: 拿掉补丁必崩、装上正常。

---

## v1.35(09-19 发布, 一键更新 / 完整包 ADAssistant_win64_v1.35.zip)

- **队友刚选完又被推荐一手**: 队友落子后快扫立刻计入这一手, 但游戏高亮慢一拍还停在他身上 → 旧版当他"还在选", 接着给他算下一手(常见表现: 推荐大招, 其实他/旁边队友都拿过了)。
  现在高亮座位的那一手已算进已选手数时当他已选完, 直接算下一位。09-18 日志 7 次(团队/单人都有)。回归 test/highlight_lag.js(旧失败/新过)。
- **团队模式给对面算**: 下一个队友在识别里已选满时, 旧版会跳到顺序里下一个有空位的人(常是对面, 还标成"队友")。现在按实际被算的人挑目标。test/team_skip_full.js。
- **提示文字**: 组合版 → 「⚡GPU版 已算完 / 计算中…」; 还没轮到的预估 → 「🧠预估(网络档) · 轮到时再用显卡算」; 技能后「组xx%」→「GPUxx%」; 设置面板按钮改名「⚡ GPU版(本机显卡)」。

## v1.34(09-19 发布, 一键更新 / 完整包 ADAssistant_win64_v1.34.zip)

- **组合版轮到自己时不启动**: 预估阶段只算网络档;真轮到自己时, 如果局面(已拿走的东西)和预估那一刻相同, 旧版直接沿用预估结果("提前算好的结果直接用"),
  组合版从不启动 —— 面板一直是「网xx%」, 等多久都不变。现在"是不是预估"算进局面签名, 轮到自己必补算组合版(显卡上约 0.3~1 秒)。
  起因: 09-18 晚 7 局日志, 开组合版的人自己回合 45 次沿用预估 vs 22 次真算;落子时显示组合版的 85% 照第一推荐选, 网络档仅 66%。
  回归测试 test/preview_then_turn_combo.js(旧代码失败/新代码通过)。

## v1.33(09-18 发布, 一键更新 / 完整包 ADAssistant_win64_v1.33.zip)

- **组合版抢显卡**: 启动时把插件进程的显卡调度优先级依次试「实时 → 高 → 较高」(D3DKMTSetProcessSchedulingPriorityClass, OBS 同款), 读回写日志 `gpu 显卡优先级: 普通 → X(…; 管理员=是/否)`, 面板组合版参数下显示。
  起因: 825204e14c87(RTX 3060 + 4K) 预热每次调网络 16ms, 进游戏后 ~1000ms(组合版 13 次算完/15 次超时/8 局退回网络档) —— 游戏占满显卡且在前台, 插件被排到后面。
  实时档要管理员(OBS 同样), 非管理员拿到哪档看系统;没拿到实时时面板提示可右键「以管理员身份运行」。**Windows 未实测**, 看下次日志里的档位和游戏中每次调网络耗时。
- 新文件 gpuprio.js(PowerShell 调 gdi32, 无原生依赖, 失败只记日志)。

## v1.32(09-18 发布, 补丁 ADAssistant_patch_v1.32.zip;**这是最后一次需要手动覆盖的补丁**)

- **一键更新**: 设置面板最上面新增「更新」区(托盘菜单第二项也有)。插件启动 20 秒后、之后每 3 小时自动查一次服务器,
  有新版本时面板高亮、托盘出现「⬆ 有新版本 vX — 点击立即更新」;只提示, 不会自己装。点「立即更新」:
  - 拉清单 `/ad/dl-022c0c58e9cd/upd/latest.json`(每个文件的 sha256), 本地逐个比, **只下载变了的文件**(按内容寻址 `upd/obj/<sha256>`, 从哪一版升都行);
  - 全部下完且校验通过才开始替换;旧文件备份到 `%APPDATA%\ADAssistant\update\backup`;
  - Windows 正在用的 .dll/.node 盖不了 → 改名挪开再放新的, 下次启动删;package.json(版本号)最后换;中途出错把已换的全部还原;
  - 装完自动重启插件。选技中点更新会先弹确认。
  - 清单的 electron 版本和本机不同(换 exe 本体)或安装目录没写权限 → 不自动装, 按钮变成「打开完整包下载」。
- 发布流程: 完整包解出来的 `resources/app` → `node tools/publish_update.js <resources/app> dist/upd --full ADAssistant_win64_vX.zip --notes 说明.txt`
  → 先传 `obj/` 再传 `latest.json` 到腾讯云 `/usr/share/nginx/html/ad-dl/upd/`。
- 测试 test/updater.js(本地假服务器: 只下变了的 / 篡改校验失败不动本地 / 占用文件改名 + 下次启动清理 / 替换一半出错全还原 / electron 不同要完整包 / ../ 路径拒绝 / 服务器不通);test/panel.js 加更新区 6 条。

## v1.31(09-18 发布, 补丁 ADAssistant_patch_v1.31.zip, 含 v1.28~v1.30)

- **新打分模型(与网站同日上线)**: orig+7.41d+7.41e 共 62.7 万局训练, 20 个重抽样模型平均折回 K=96, 克制/簇对/配比三附加项在新底座上重拟合。
  7.41f 2.1 万局(训练没见过)上误差 0.5850→0.5811、判对 68.4%→69.0%;冷门组合两半一致 0.72→0.82。含条件收缩(莉娜×月蚀这类"证据几乎全来自带月光的座位"的配合, 座位缺月光时按缺它的样本数打折)。
- **网络档/组合版换新网络 netB300**(按新打分自博弈续训 300 轮):对新现役 纯网络 54.3% / 组合版(K8×R64) 60.1%、赢 89% 局。data/net550.* 不再使用。
- **网络档/组合版也守硬依赖规则**(和现役同一张 engine/server/hard_deps.json: 月蚀←月光、魂之挽歌←影魔本体): 座位里没有条件件时, 依赖者出手概率 ×e^-4。
  理由(用户): 先拿条件件是弱占优 —— 对手不截时两种顺序一样, 截了先拿条件件严格更好;真人 83 万局同座两件都有的 97% 先拿月光。
- **引擎同步网站版**: engine/server/mcts_fast.js(硬依赖罚 + 条件收缩)、ai.js、engine/public/ad_score.js(面板标「缺X ×g」)。
- 测试: test/net_cases.js(纯 JS 网络 vs PyTorch 40 局面, 首选 40/40, 出手概率差 <0.4%);preview_net / preview_combo 端到端通过(Linux CPU)。

## v1.30(09-18 发布, 补丁 ADAssistant_patch_v1.30.zip, 含 v1.28/v1.29)

- **修: 个人权重不是"团队" + AI 档位网络档/组合版 → 识别线程崩溃、画面卡在最后一帧(09-18 飞刀局, 左上角 `TypeError ... reading 'toFixed'`)。**
  网络档/组合版排序不算个人分 `r.pm`, 但写日志那行在个人模式下读 `r.pm.toFixed` → 抛错。改为只在算了个人分时才写;推荐本身不变(网络档/组合版本来就不用个人权重, 日志里注明"不生效")。
  复现: 个人权重 3 + 网络档, 轮到"下一手是我"即崩;修后同步骤通过。
- **识别线程崩溃兜底**: 线程退出 → 1 秒后自动重启(10 分钟内最多 5 次) + 立刻上传日志(`crash`, 仍听自动上传开关)。以前崩了就永远卡住、日志也传不上来。
- **组合版提速/诊断**(起因: 8252 那台 3070 组合版比别人慢 10 倍, 截屏也慢 12 倍):
  - 优先独显: Chromium `force_high_performance_gpu` + 本程序 exe 写 Windows 图形偏好"高性能"(HKCU UserGpuPreferences, 下次启动生效, 用户自己设过的非节能值不改)。
  - 组合版预热: 开组合版就在后台建会话;DirectML 逐块显卡(0~3)实测, 选最快的;固定批大小(freeDimensionOverrides, 图 369→209 节点, 结果与动态批逐位一致)。
  - 新任务来了取消旧任务(旧的不再占着显卡跑到超时)。
  - 修"组合版出错: undefined"(其实是已选完没东西算)。
- **日志新增**: 显卡型号/驱动/Chromium 加速状态、系统显卡列表、截屏测速(CPU 画布 vs 显卡画布)、组合版每轮耗时拆解(推进手数/网络调用次数/首次·中位·最慢/打包·运行·编码·抽样)、组合版作废原因与耗时。

## v1.29(09-17 发布, 补丁 ADAssistant_patch_v1.29.zip, 含 v1.28)

- **「AI 档位」加第三档: 🧪 组合版(本机显卡)。** 推演框架 + 推演里双方都按网络出手;对现役 AI 模型口径 57.2%(赢 76% 局), 对纯网络 67.5%(GPU 版 K8×R64 实测)。
  实现 combo_local.js(现役同款增量引擎批量推演 + ONNX 网络 data/net550.onnx, onnxruntime-node 1.20.1 只带 win32/x64, Windows 走 DirectML, 任何 DX12 显卡) + combo_worker.js 独立线程(不卡识别)。
  参数(设置面板): 每手前 K 个候选(默认 8)× 各推演 R 局(默认 16)× 每手上限秒数(默认 8)。超时 → 下一手 R 减半;R=8 仍超时 → 本局改网络档(下一局重试);显卡/运行库不可用 → 改网络档。日志里有"组合版算完 K×R 设备 用时(编码/网络)"。
  预估(还没轮到你)仍用网络档排序;轮到你/马上轮到时算组合版, 算完同一手重排一次, 格子写"组xx% · 原推演胜率"。
  Linux CPU 小参数端到端通过(test/preview_combo.js);**Windows 显卡速度未实测**, 以用户日志为准。

## v1.28(09-17 发布, 补丁 ADAssistant_patch_v1.28.zip)

- **设置面板新增「AI 档位」: 现役 / 🧠 网络档(默认现役)。**
  网络档 = 自博弈训练(GPU, 约 225 万局 PPO)第 550 轮的选技网络, 对现役 AI(走子取平均)换边 100 局模型口径胜率约 52%, 会断人(蝙蝠骑士局对面会断无影拳)。
  排序: 网络出手概率 ≥5% 的候选按网络概率排在前面, 其余按原推演胜率排;格子上写"网xx% · 胜率", 胜率数字仍是原推演算的(网络自身估值误差约 5 个点, 不显示)。
  纯 JS 推理(netinfer.js, 无原生依赖), 权重 data/net550.bin(8MB), 每手约 0.2~0.5 秒, 与 PyTorch 原版前五名及概率逐一一致(test/net_mode.js)。
  池子不是 12/36/12(识别缺件)或模型载入失败时自动退回现役排序, 只记日志。

- **设置面板新开关「轮到你时」(默认关)**:预估下一手(没轮到你)时, 黄色推荐条下面多一行灰色备注 ——
  把中间那几手按推演同一套走子规则模拟 200 遍, 给出主推荐前三「轮到你时还在」的概率, 以及按主推荐名次往下数、届时最可能拿到的首选。
  起因(09-16 局): 二楼拿完蝙蝠骑士后一直显示下一手巨力挥舞, 5 手后就被队友拿走 —— 预估口径是"假如现在就轮到你"。
  **主推荐一个数不改**(用户定: 只作备注);轮到你/马上轮到时不显示。耗时 10~150ms, 在识别线程里算, 出错只记日志不影响推荐。
  测试 test/preview_arrive.js。改动: worker.js(arriveNote) / main.js(settings.arrive 落盘) / settings.html / overlay.html。

## v1.27(09-15 发布, 补丁 ADAssistant_patch_v1.27.zip / 完整包 ADAssistant_win64_v1.27.zip)

**英雄卡也不再只看亮度:和它自己锁池时的卡面比**

v1.26 的图标规则只做了技能格(有图标库)。英雄卡没有库 —— 锁池时英雄卡是靠同一排技能反推的, 卡面从没比对过。
现在 `maskedVec` 在第一次清楚看到卡亮着时存一份掩码归一化向量(掩掉覆盖层会画到的区域), 之后每帧算相关性(`selfScore`),
`heroVerdict`:相关性 <0.35 + 没被点亮 + (亮度判"看不清" / 亮度比 <0.75) → 选走;相关性 >0.8 + "看不清" → 没变。
真机 1080p 238 张英雄卡:选走判对 90/106 → 93/106, 误报 0;2K 96 张不变。斯温卡(13:20 那局)选走后亮度规则要 6 秒才判出, 这条 1~2 帧。
轨迹里新增 `icoH`(英雄卡自相关分), 老轨迹没有 → 回放时这条不生效。重锁同一池子时 `heroRef` 随其它状态一起带过去。
回归:full_draft SEED 1~5、late_pair/restart/flaky/fast/baseline/trace_roundtrip 全过。

---

## v1.26(09-15 发布, 补丁 ADAssistant_patch_v1.26.zip / 完整包 ADAssistant_win64_v1.26.zip)

**"格子被选走"改成看图标还在不在 + 同局自标定, 不再靠每台机器各调一套亮度阈值**

用户 09-15:"每个人画面设置、分辨率都不一样, 每个人单独一个阈值不对"。焦渴那局(12:26)是最清楚的例子:焦渴本来就是暗红图标,
锁池时亮度 52, 被左3 拿走后 32, 比值 0.6 刚好判"没变", 之后 4 分钟一直是给用户的第一推荐;而它自己图标的匹配分从 0.83 掉到 −0.14。

1. `recog.iconScore`:每格"还像不像自己锁池时那个图标"(去均值单位化模板匹配, 与亮度/缩放/反光无关)。判据只用比值(现在 / 第一次清楚看到它亮着时),
   `skillVerdict` 综合:图标没了 + 没被点亮(不是提示框白字) + (亮度判"看不清" / 亮度比 <0.75 / 颜色淡了 / 长得像同排已确认选走的格子) → 选走;
   很暗 + 图案只剩一半像("鬼影":冰火交加/飘忽不定) → 选走;图标清清楚楚在 + 亮度判"看不清" → 没变。
   真机 1080p 842 格(目视修正真值):选走判对 364/398 → 387/390, 误报不增(27 个全是 08:59 整板变暗那一次);2K 361 格完全不变。
2. **同排自标定**:同一排已确认选走(配对过 / 连续暗 12 帧)的格子此刻的亮度就是"这台机器上选走长什么样"的样本, 其它格子直接和它比。
3. **掩码**:真机截图证实**插件自己画的推荐框/底部标签条/序号会进截屏**(那台 1080p 机器上 setContentProtection 没生效, 见下), 底部标签条占格子 38%。
   图标匹配只看外圈 8% 以内、底部 40% 以上、左上角 30%×30% 以外的区域, 锁池参考分同一掩码。
4. 鼠标停在某格上时(游戏弹技能介绍框), 光标右侧 560/上下 320·280(1440p 尺度)范围内的格子不用图标规则(介绍框文字盖住图标)。
5. 同一帧 ≥10 格一起"新变黑"= 整块被盖住(菜单/计分板/转场), 先当看不清最多 8 帧再放行(以前 5 帧就当选走, 一批"不当落子";超过 8 帧仍暗 = 真的一起黑了, 中途打开插件那种)。
6. 轨迹里新增每格图标分 `ico`(回放要用;老轨迹没有 → 图标规则不生效, 回放结果与 v1.25 一致)。

**顺带发现(没改)**:朋友那台 1080p 机器上覆盖层**能被插件自己截到**(draft_end 截图里有推荐框和"52.1 +0.0"标签)。
`setContentProtection` 对应 Windows 的 WDA_EXCLUDEFROMCAPTURE, 需要 Win10 2004 以上;老系统上只是不生效。
亮度统计取格子内圈 60%, 受影响不大;但面板/头像/本人绿框那些读数可能被画上去的东西干扰。下一步:启动时自检(画一个标记看截图里有没有), 有就把画过的区域从识别里剔掉。

回归:full_draft SEED 1~7(SEED 3/7 各差 1 手是改前就有)、late_pair/restart/flaky/fast/baseline/trace_roundtrip 全过。
工具:`tools/audit_taken/feat.js`(截图上量各特征 + 新旧判定对比)、`crops.js`(把争议格子裁成拼图目视定真值)。

---

## v1.25(09-15 发布, 补丁 ADAssistant_patch_v1.25.zip / 完整包 ADAssistant_win64_v1.25.zip)

**推荐里出现已经被选走的技能:两处根因(服务器日志 8 局审计)**

用户 09-15:"经常有推荐已经选了的技能"。把 logup 收到的 8 局真机轨迹(09-14/09-15, v1.21/v1.22)逐帧回放, 以每格像素为真值统计
"真被选走了、但引擎还当它可选"的时间:8 局共 504 秒(平均每局 63 秒), 期间这些技能随时可能进推荐;前三里实锤的例子:
月刃(10:47 那局)被左1 拿走后 20 秒里一直是我的第 2 推荐;粘性炸弹(09-14 16:14 那局)被选走 78 秒引擎仍当可选。

1. **快通道判定被第一帧完整识别放掉。** `worker.js` 慢通道里"清楚看到亮回来才放"写成了 `brightRun>=1 && darkRun==0`:
   落子前那格一直亮着(brightRun 早就 >=1), 完整识别只要没判成"被选走"—— 判"看不清"也算 —— 快通道那条就没了。
   而棋盘下排的格子被选走后是带颜色的反光黑(月刃 24/40 亮度、饱和度反而从 0.21 涨到 0.52), 完整识别按"颜色没掉"连判 12 秒"看不清"。
   现在:只有完整识别**清清楚楚看到它亮着('N')连续 2 帧**才放, 否则一直算已拿走, 最长 20 秒(原 6 秒)。
2. **"会闪"的格子永远进不了"已拿走"。** `recog.js` `solidDark()` 要求 `!flaky`:赛前阶段闪过几次被标 flaky 的格子, 之后真被选走、
   又没配上面板(不当落子)时, 连续暗 60 多帧引擎还当它可选。现在连续暗满 12 次完整识别(十几秒)就算拿走, 会闪的格子撑不过 12 次。

回放同一批轨迹(`tools/audit_taken/`):漏算 504 秒 → 259 秒;剩下的主要是每手 3~9 秒的确认延迟(同帧多格变黑要等 5 帧、看不清的帧不计数)
和局末转场。`test/full_draft.js`(SEED 1~5)/`late_pair`/`restart`/`flaky`/`fast`/`baseline` 全部与改前一致(SEED=3 的照明火箭是改前就有的)。

顺带看到但没改:局末画面转场时会出现"撤销"(战意/齐天大圣/小小 格子亮回来), 之后被当可选推荐 —— 都在选完之后, 不影响选人。

---

## v1.24(09-15 发布, 补丁 ADAssistant_patch_v1.24.zip / 完整包 ADAssistant_win64_v1.24.zip, 含 v1.23)

**直播模式:让直播/录屏软件能抓到覆盖层(可开关, 默认关)**

用户 09-15 转来的诊断:`createOverlay()` 里 `overlay.setContentProtection(true)` 调 Windows `SetWindowDisplayAffinity`,
把覆盖窗从**所有**屏幕/窗口捕获里排除 —— 自己屏幕上正常显示, 视频号直播助手无论推流屏幕还是推流进程都抓不到。

难点:这个排除对所有捕获一视同仁, 插件自己的截屏也靠它才看不见自己画的框(胜率条压在格子底部、徽章压在角上, 进了截图就污染识别)。
所以不能简单放开。做法:
- 直播模式下插件自己改抓**游戏窗口**(`desktopCapturer` 的 window 源, 找标题 Dota 2 的窗口), 窗口捕获只拿那个窗口自己的画面,
  盖在上面的覆盖层不会进来;**接通之后**才放开防捕获。没找到游戏窗口时先抓屏幕、覆盖层继续隐身, 每 5 秒再找, 进游戏后自动切换。
- 独占全屏的游戏窗口捕获只能拿到黑屏:扫描帧连续 40 帧近黑 → 退回屏幕捕获 + 重新防捕获, 状态行提示改无边框窗口。
- 逐帧截屏那条兜底路也跟着抓窗口(要给所有窗口出缩略图, 慢, 但本来就是兜底)。
- 开关在设置面板「直播模式」和托盘菜单「📺 直播模式」, 存盘;状态行和面板都写当前捕获到的是窗口还是屏幕。
- 重开视频流时旧页面的 "ended" 消息 3 秒内忽略, 不再被当成流坏了退到逐帧。
- **没在 Windows 真机验证**:窗口捕获(WGC)在无边框窗口下是否真的不含覆盖层、Win10 是否会给游戏窗口画一圈黄边, 都要真机看。

---

## v1.23(09-15 发布, 补丁 ADAssistant_patch_v1.23.zip / 完整包 ADAssistant_win64_v1.23.zip)

**推荐框换成朋友改的配色 + 自己能调 + 设置面板(双击托盘图标)**

用户 09-15 发来朋友改过的截图:第一名红、第二名黄、第三名紫、4~8 名浅蓝, 比原来的金/银/铜 + 青色更醒目。
截图取色:`#ff4d4f` / `#ffd038` / `#c263ff` / `#80d5fb`, 现在是默认配色。
同时提的:"设置一个功能可以自己调色, 或者把插件设置一个面板, 双击可以打开, 要不全在右下角调也不方便"。

- **配色可调, 存盘**:颜色(前三名各一色 + 4 名以后一色)、画框画到第几名(3~10, 默认 8)、框的粗细(0.5~2 倍)、
  后面的名次写不写胜率。存 `settings.json` 的 `look` 字段, 重启不丢;坏值逐项回默认。
- **设置面板**(`settings.html`):双击托盘图标打开, 托盘菜单第一项也能开。托盘菜单里所有开关都搬了进来
  (隐藏/显示、单人/团队、组合明细、我是几号位、个人权重、暂停、重新识别、运行模式、内核、自动上传、
  立即上传/保存截图/打开日志/退出), 加上快捷键实际生效表。改颜色时面板里有预览, 选技中的游戏画面同步变。
- **画框代码合并成一份**(`frames.js`):覆盖层和面板预览用同一个 `drawRow`, 预览里看到什么游戏里就是什么。
  测试 `test/overlay_look.js`(默认色 / 改色即生效 / 画到第几名 / 坏配置回默认)、`test/panel.js`(面板脚本假 DOM 跑一遍)。
- 面板是置顶普通窗口:无边框窗口化的游戏上面能直接看到;独占全屏会被切出去, 和打开托盘菜单一样。
- **没在 Windows 真机上看过面板**, 这是这版最需要反馈的。

---

## v1.22(09-14 发布, 完整包 ADAssistant_win64_v1.22.zip)

**插件认错"你是几号位", 推荐整局算给了别人(1080p 基本每局都错)**

09-14 朋友 1080p 那局坐右5, 插件整局认成右2:日志每一行"我 R2", 推荐全是给右2(工程师)算的,
看起来就像"以为他没选大招一直乱推荐"(右2 那几件确实没有大招)。他自己的术士和混乱之祭其实记对了。

原因:游戏给本人面板画一圈亮绿线, 最清楚的是**英雄头像那一侧**的竖边。以前量的是面板上下两条边 + 技能那一侧的平均颜色,
要求"绿度" > 25 —— 头像那一侧被故意排除了, 那三条边上根本没有这条线, 10 个面板绿度最高只有 6~11, **门槛从来没过过**;
过不了门槛就拿**锁池第一帧"最绿"的面板**当自己并整局沿用。逐局核对了全部有日志的截图(只拿同一台机器的日志对它自己的截图):
2560 21 张全对、4K 16 张对 15 张(碰巧), 1080p 今天这局错, 09-11 两局也和截图里的真实座位对不上。

**根治, 三层一起改**

- **量对:** 量头像那一侧边缘的"绿线覆盖率"(边缘左右各 6px 里, 最绿的三列各有多大比例的行是亮绿)。
  85 张真实截图(1080p / 2560 / 4K / 半分辨率):本人 0.49~0.82, 非本人最高 0.29(绿头发的琼英碧灵头像, 是一片散开的绿, 没有贴边的线);
  轮到自己选时绿框也还在(10 张)。是比例, 和分辨率无关。
- **不猜:** 每次完整识别投一票, 最近 6 次里同一座位 3 票、别的座位都不到 2 票才认定;**认定前"我"为空, 不出推荐**,
  状态行写"还没认出你是几号位"。翻牌、转场看不到绿框的帧弃权, 已认定的不变;单帧别处出现绿框不改。日志记 `[me] 认出本人座位 R5`。
- **能纠正:** 托盘「我是几号位」可以手动指定, 手动优先;只对这一局有效, 换一局自动恢复成自动识别。

另外:测试从来没测过"我是谁", 所以这个问题从 v0.2 一直带到 v1.21。现在有 `test/me_seat.js`(真实截图 + 投票逻辑 + 真 worker 合成局)、
`test/overlay_me.js`(状态行三种状态), 真机基准每帧记 `me` / `meRim`;轨迹里记每个面板的绿框分数, 回放和线上一致。

**验证**

| 测试 | 结果 |
|---|---|
| 真实截图认本人(用户确认的右5 + 7 张有旧日志互证的 2560) | 全对;本人最低 0.74、非本人最高 0.29 |
| 同一局几帧座位一致(3 组) / 任何一帧出现两个强绿框 | 一致 / 0 帧 |
| 投票逻辑 12 种情况 + 真 worker 合成局(没绿框不推荐、自动认出、手动指定、改回自动、换局作废) | 全过 |
| 合成整局 SEED 1~4 / 状态机、预估、模式、落子重算、轨迹、组合明细 | 与 v1.21 逐字相同(SEED 3 那件改前就错) |
| 记录↔回放 | 逐字相同, 观测缺口 0 |
| 真机基准 25 帧 | 除新增的本人座位字段外零变化 |

合成画面(`test/synth.js`)的本人绿框也改成和真实游戏一样:只画头像那一侧、轮到自己时也在。

这次**没改**的:快选完时"第几手"会被没人要、变暗的格子拉高, 我选满后最后一轮可能短暂显示别人的预估(标着"我")——
影响小(发生在自己已经选完之后), 另议。

## v1.21(未发布)

**打完一局自动上传日志(托盘可关, 默认开)**

以前排查问题要一个个找人要日志。现在插件在这几个时刻, 把"上次上传以来"的日志 + 截图 + 轨迹打成 zip 传到作者的服务器存档:
一局选完回到空闲 20 秒后;选技中 3 分钟以上没到选完就结束;连续锁池失败 ≥8 次、之后 2 分钟没有新的失败(09-13 那种一直锁不上的局);
托盘「立即上传本次日志」。两次自动上传至少隔 5 分钟, 单包 ≤25MB。

- **插件里没有任何密码。** 插件带的上传标识不算秘密, 别人拿到只能往里传, 读不到任何东西。
- 服务器(`server_logup/logup.py`, 独立服务, 不碰游戏服务):只收 zip、包里只许日志/截图/轨迹文件名、解压总量上限、每 IP 每天 30 次、存档总量 1.5GB 滚动清理、存档不对外。
- 传的是整屏截图和日志(日志里有 Windows 用户名路径);走 HTTP 明文。不想传就在托盘里关掉, 设置存盘, 重启不会自己打开。

**2K / 4K 开局一直锁不上池子(09-13, 三次重启都一样)**

日志 13 次拒绝, 行裕度每次都是 0.027:育母蜘蛛那一行被认成了祈求者(池子里根本没有卡尔)。
原因是"标准技能"区右上角那个黄色 L 形角框(游戏自带, 每局都在)和格 18 连成了一块, 检出框被撑成 84×78、中心偏 10px,
格 18 的麻痹之咬只剩 0.14(正位 0.64);卡尔有 720 种技能排列, 另两格"都有点像"就反超了。
不是鼠标、不是插件自己的覆盖层 —— 截屏排除了覆盖层, 锁池前覆盖层也不画框。

**这不是这一局才有的:** 17 张历史 2K 开局帧里格 13 有 15 张、格 18 有 13 张被角框撑歪, 以前靠同一行另两格撑住没出事
(09-12 22:44 那局格 18 只有 0.11)。4K 先缩到 2560 再识别, 所以一样;1080p 没有这个问题。

**根治:检出框不再当比对框**

- 检出只用来求整个棋盘的偏移;每一行格子的宽/高/偏移在锁池时**按图标标定**(同一行几格一起投票, 不预设是谁的技能)。
  以前还有两处也是框的问题, 一并好了:行 4 大半格子被撑高(这几格长期 0.4~0.6, 现在 0.7~0.9), 1080p 大招行连上文字标签。
- **锁池后冻结棋盘位置。** 以前每帧重新检出, 到后半局格子少了偏移会漂(选完时 y 偏 8px):
  09-12 那局选完时没被选走的 12 个亮格, 按每帧偏移只有 2 格认得出, 冻结后 12 格全对。
- **全黑的格子(已被选走)不参与认英雄。** 框改对之后暴露的:行 4 左两个黑格上的噪声让剃刀以 0.017 反超孽主(孽主的火焰风暴亮着 0.87)。
- 锁池/拒绝时日志多两行"诊断":最弱的是哪一行、三格各多少分、哪些检出框被弃用、各行标定前后的分数。以后单看日志就能定位。
- 代价:每次锁池多约 0.9 秒(只在锁池时, 追踪每帧反而省掉了格子粗检)。

**验证**

| | 改前 | 改后 |
|---|---|---|
| 09-13 事故帧 | 拒绝, 裕度 0.027, 池里有卡尔 | 锁池, 裕度 0.13, 育母蜘蛛那行 0.285、格 18 0.84 |
| 17 张可锁真机帧的最小行裕度 | 0.06~0.26 | 全部持平或上升, 0.08~0.38(1080p 开局 0.06→0.38) |
| 池外高分(池子读错的铁证) | 3 | 1 |
| 同局配对 / 吻合率 | 7 对, 83%~100% | 8 对, 88%~100% |
| 合成整局 SEED 1~4 | 1~3 与改后逐字相同 | 40/40/39/40(SEED 3 那件改前就错) |

新回归 `test/box_align.js`;`test/fixtures/` 加了 09-13 和 09-12 两张事故帧。
顺带更正 `docs/testing.md` 的一条旧结论:`start_1080p_0911` 和 `sheen_1080p_0911` 其实是同一局, 当年"棋盘重排了"也是每帧偏移漂出来的假象。

**还没做**(第 2~4 层, 见 docs):格子附近小范围找最佳位置、排列多的英雄打折 + 英雄头像作为独立证据、拿不准时先锁池把那一行标待定。
已知小退步:`snap_20260912_152126` 大招格 5(光之守卫 灵魂形态)从"救回"变成"未知"(同时 `152730` 的格 5 从未知变成认出)。

---

## v1.20

**选技时显示双方十人的组合明细**(朋友 cz3900 提的 PR #1)

十个玩家面板朝棋盘的一侧, 各竖着显示一张小卡:基础、配合、队友、克制、合计, 正分绿、负分红;
底部写英雄认没认出来、技能认出几件, 认不出的标「待认」。托盘菜单「显示双方组合明细」可以单独关掉(默认开),
不影响技能推荐。分数和选技推荐用的是同一个模型, 但它是**这个人手上已有东西的组合分**, 不是个人胜率,
队伍级的构成与配比项也不摊到个人头上。

合并前拿你们两台机器 09-12 的真实轨迹回放过(12 条 / 2732 帧, 观测缺口 0, 每帧评分 p95 ≤0.41ms)。
英雄在模型里的 key 是 `hero:<id>`, 它映射对了 —— 直接喂英雄原名会被模型静默丢掉, 这是之前离线打分踩过的坑。

**组合明细出错, 不能把整个插件拖垮**

它只是显示层, 但原来有两种方式能让插件整个失效:启动时评分模型加载失败, worker 直接起不来;
某一帧评分出错, 那一帧的状态和推荐都发不出去(它和主状态在同一个 try 里, 而且排在发送之前)。
现在出错只丢这张卡, 识别、状态、推荐照常, 同一类错误只记一次日志。

用往 worker 里注入故障的回归测试(`test/player_scores_guard.js`)验证, 先确认它在没修的代码上会失败:

| 注入的故障 | 修复前 | 修复后 |
|---|---|---|
| 评分模型加载失败 | worker 起不来 | 照常锁池, 状态照发, 错误记 1 次 |
| 每帧评分出错 | 锁池后状态发出 **0** 次 | 照常锁池, 状态照发, 错误记 1 次 |

**合成测试找不到图标时, 报错而不是死循环**

`test/synth.js` 按仓库外的相对路径找图标, 仓库不在 `ad-draft/app` 这个位置时候选为空,
挑英雄的循环永远凑不满 —— 同步死循环、一行输出都没有, 看起来像被测的代码卡死了。
审 PR 时正是踩了它, 差点把一个没问题的 PR 判成"让 worker 卡死"。现在立刻报错并说明缺哪个目录。

---

## v1.19

**同一局，两个人看到的推荐不一样**

09-12 晚上两台机器打同一局，其中一台把蓝猫的「超负荷」认成了「真空」。
60 格里只差这一格，但池子一错，引擎局面就不同，两人从头到尾看到的推荐都不一样。

根子在读池子的最后一步"以图标为准"：某格全库最像的图标如果明显好过行约束的答案，就改判成它。
这一步是必要的（技能不足 3 个的英雄会由别人的技能补位），但它原来**不看这个"最像"有多可信**。
那一格两台机器贴框收敛到 72×62 和 84×78，后者把边框裁了进来，全库第一翻成「真空 0.73」——
而它在全库里只领先第二名 **0.088**，根本就是没认出来。

现在要求：覆盖行约束的改判，全库第一必须明显领先它自己的第二名（0.15）。
真机 14 次改判分得很开——合法补位领先 0.408~0.590，五次错误改判全在 0.012~0.088。
22 张真机截图上另有两处跟着改对，而且判据很硬：修后的答案都是**这一行自己英雄的技能**
（巫医那行的「麻痹药剂」、帕格纳那行的「幽冥守卫」，原来被改成了不在池子里的英雄的技能）。

**2.0 内核：不肯分配"没有面板证据"的东西**

有一局 2.0 让右1 连英雄都没分到、六件技能全是"无归属"。
最小费用流的增广条件写成了"收益 > 0 才继续"，而一件棋盘看见了、面板始终没画出来的东西整行都是 0，
收益恰好为 0，于是它永远留在原地。座位容量（1 英雄 + 4 技能）本来就是硬上限，不需要靠收益兜底。

改完之后，用一个**不需要人工标注的判据**（每个座位最后必须恰好 5 件，这是游戏规则）
在两台机器 10 局 2705 帧上重算：**2.0 从 82/100 升到 86/100，反超 1.x 的 85/100**，
四局各涨 1、没有一局变差。

> 值得记一笔：同样这套 2.0，在合成语料上一直赢 1.x 5 件技能，真机上一开始却输 3 个座位。
> "合成测试测不到真机的主要错误来源"这条教训，这次轮到了 2.0 自己头上。

---

## v1.18

**两个互相独立的开关:运行模式 / 状态估计内核**

以前只有一套行为。现在托盘菜单下半部分多了两组单选, **互不影响**:

| 开关 | 选项 | 管什么 |
|---|---|---|
| 运行模式 (F11) | 正式 / 🔬 测试 | **只管截图和日志**, 不改变任何判断 |
| 状态估计内核 (F12) | 1.x 稳定 / ⚗ 2.0 试验 | **只管判断**"被拿走了没有 / 归谁" |

测试模式在九类"关心的点位"自动存截图并写一条解释:池外高分(池子读错的铁证)、强行按图标认、
手数上限兜底、不当落子的暗格、英雄座位改判、走了兜底配对、2.0 说没把握的归属、
两个内核结论不一样、以及每 10 手一张底片。每类各 12 张、半分辨率。

**测试模式不会干扰它自己要测的东西**:PNG 编码要几百毫秒, 放在工作线程里会把识别卡住
(第一版实测 27 帧只来得及做 21 次完整识别)。现在工作线程只做一次像素拷贝就把裸缓冲转移给主进程,
由主进程去编码落盘 —— 开不开测试模式, 完整识别的帧数完全相同。

**测试模式下两个内核并行跑**, 谁驱动输出由内核开关决定, 另一个只在旁边算。
结论不一样时日志里出现 v2 行并自动存一张截图 —— 这是最能看出两版差别的地方。

**第一名的框:亮金 + 加粗**

原来的 #ffcc33 偏琥珀, 而游戏棋盘上本来就有大量暖色图标, 第一名不够跳。
改成 #ffe24a, 框从 6px 加到 9px, 外发光 18 → 30。银(第二)和铜(第三)**完全没动** ——
只动第一名, 名次层级反而比原来清楚。

**观测轨迹：真机对局终于能回放了**

在此之前，日志里只有**结论**（某技能归了谁、某格撤销了），没有**观测**。
后果是：每打完一局，那一局的数据就扔掉了；任何一版新代码都没法在你打过的真实对局上离线验证。
这是这个项目最贵的一处可测试性缺陷——比归属层的补丁堆积更贵。

现在 `worker.js` 默认把每帧的观测写进日志目录（4.2 KB/帧，一局约 1 MB，单局上限 40 MB，
跟日志一起 7 天后清理）。回放**比线上快 600 倍**。

验收标准是**逐字相同**：读像素得出的归属和日志，必须和纯从轨迹回放（完全不碰像素）得出的一模一样。
11 种条件全部通过、观测缺口 0。线上路径本身零行为改变——v1.17 与打完补丁的版本在 8 个种子上逐字相同。

**常驻真机基准**

22 张真实截图跑成一张会自己报警的成绩单（`node test/baseline.js`）。
在此之前所有核对都是临时脚本量完就扔，改坏一次全靠 A/B 撞见。
判据全部是结构性的、不需要人工标注——上线第一天就查出 `test/census_real.js`
一直拿**两局不同的对局**当同一局的参考帧/中局帧在用。

**2.0 状态估计（`v2.js`，默认不启用，可在托盘菜单切换）**

详见 `docs/design-2.0.md`。14 局合成轨迹：技能归属 555/560（1.x 是 550/560），英雄打平 140/140，
"认定被拿走"后撤回 0 次，剩下 5 个错全部是两两对调、且全部被置信度标了出来。
代价是判定慢一到两帧。

**真机验收(09-12 第一局带轨迹的真实对局，242 帧)**：选技过程 228 帧里只有 10 帧(4.4%)有分歧，
每次都是单独 1 件、持续 1~3 帧(就是那 1~2 帧的判定延迟)；**选满 50 手那一刻技能归属 40/40 一致、
英雄 10/10 一致**。2.0 线上开销均 4.1ms，同期完整识别均 74ms(5.5%)。全场 0 错误 0 警告。

这一局还抓出两个**只有真机数据才会暴露**的真问题，都已修(合成测试跑了 14 局 1847 帧，一次都没碰到)：

- **全局归一化的正反馈回路**："跟这一帧整体比"那条，分母取的是"还没被拿走的格子"的亮度中位数。
  选完时 60 格里 50 格真被拿走，中位数本就低；2.0 一旦错退一格，那格就进分母把中位数拉得更黑，
  于是更多格子看起来"没被拿走"——退得越多退得越快。真机上 50 件退到只剩 22。
  分母下限钉在 0.6(有出处：真机实测遮挡只把亮度压到 0.62~0.9)，修后同一轨迹只掉到 44，
  且**选技过程完全不受影响**(选满 50 手那一帧修前修后都是 50/40)。
- **"池外高分"判据太松**：测试模式第一次跑就报三处"池子读错的铁证"，逐一查证**三处全是假阳性**
  (一处全库前二是 0.7212 / 0.7176，领先只有 0.004；一处那槽近乎全黑)。
  补上"全库第一必须明显领先它自己的第二名"，同一轨迹假阳性 7 帧次 → 0。

**默认仍是 1.x**。真机上两版打平，说明的是"可以让它上场，因为不会更差"，
而不是"它更好"——要分高下得等一局 1.x 真出错的对局。

---

## v1.17

**认不出的格子, 原来永远救不回来**

贴框那一步写着"认不出的格子跳过"。可"认不出"往往恰恰就是框没贴准造成的 ——
这给这类格子留了个死角:框不准 → 认不出 → 因为认不出所以不贴框 → 永远认不出。

实例:某局第 12 个大招格原框只匹配到 0.47, 差 0.08 没过门槛, 整局显示"未知技能";
框挪 2 像素、缩 2 像素之后是 0.76, 第二名只有 0.16。

现在去掉这个例外, 所有格子走同一条路。太暗的格子(锁池时亮度 < 60)不猜 ——
猜错比留"未知"更糟。19 张真实截图复核:两格从"未知"变成认出(人工确认认对),
没有任何已认出的格子被改动。

> 同一轮里还试了两条"更好的方法"并**否掉了**:全局单应矩阵摆位(97.9%, 不如逐格贴框)、
> 梯度方向直方图 + 颜色直方图(抗框歪但有天花板, 且与像素相关**零互补**)。
> 详见 docs/recognition.md。结论是匹配这一步已经到顶, 不再是瓶颈。

---

## v1.16

**面板空槽被当成有图标**（这版的重点）

判"槽里有没有图标"用的是绝对亮度门槛。空槽底板本身带渐变和高光，
画面一亮（转场 / 技能特效 / 翻牌动画）就会亮到超过门槛，整排空槽被当成有图标，
再硬配成池子里的技能。实测 719 个真空槽里误判 **102 个**，最高能配出 0.56 分。

日志里"某一侧 4 个面板凭空各多 4 个图标、一口气记了 16 手、还误判成新的一局"，
根子就在这里——v1.15 只是卡住了"手数不能多于棋盘暗格数 + 1"，没有治根。

改成看纹理（空底板是平的，图标有细节）。门槛按整个图标库校准：
游戏里渲染出的纹理实测是图库原图的 1.35~1.58 倍，取最保守的 1.35 换算，
513 个图标一个都不会漏。误判 102 → 11，剩下的集中在"说明浮窗盖住面板"那一种画面。

**面板槽贴框标定范围放宽**

槽的大小原来只能往大调（0~+6 像素），实测真实最优常在 +8，够不着。
现在两头都能调（−4 ~ +12），并改成先粗后细，计算次数反而比原来少。
面板匹配分中位 0.54 → 0.94。

---

## v1.15

- 手数不能多于棋盘暗格数 + 1（治"凭空冒出 16 手"的症状）
- 快通道判定的落子并入局面，防止一手被重算 2~3 次（前半局每手快 1.5~2 秒）

## v1.14

- 英雄归属的第二信号：读面板标题里的英雄名（锚点快读，15~100ms）。
  真机一局 0 次回撤 + 6 次名字确认；上一版同样长度的两局分别有 16 和 35 次回撤

## v1.13

- 已连续暗 ≥ 12 帧的格子不再回撤（治"鼠标悬停把已拿走的格子照亮"）
- 鼠标所在的格子直接判"看不清"
- 翻牌动画中不锁池

## v1.12

- 整局 50 手的压力测试（`test/full_draft.js`），并据此修了 6 条面板图标晚到引发的连锁错误

## v1.11

- 晚锁池时按回合填空失效、两个英雄坐同一个座位、低门槛补认缺保险、
  空槽门槛、重锁丢帧号——5 处隐患

## v1.10

- 检出框过大（> 1.3 倍格宽）时改用版式位置。
  此前某些格子的检出框把"终极技能"文字标签框了进去，那一格匹配分只有 0.01~0.12，
  一度被误判成"图标库过时"——**真相是框太大，不是模板旧**
- 英雄座位按时间顺序复核（治两个英雄对调）

## v1.9

- 1080p 下的四处修正：面板空槽门槛、暗格与"没认出"互补认、大招格排除法配对、英雄卡近黑判定

## v1.8

- 选技阶段之外不显示任何东西；F6 一键隐藏（识别照常运行）

## v1.7

- 左下角光面反光的格子按"颜色没了"判定——此前整类格子漏认
- 配对改用"面板图标数 − 已记账件数"，防止玩家拖动换位置造成误判

## v1.0 ~ v1.6

- 归属改为配对式（棋盘变暗 + 面板多图标），英雄按回合锚定
- 三态判定（没变 / 去色压暗 / 看不清）
- 固定随机数 + 只按落子事件重算 + 只发完整结果 = 推荐不再抖动
- 常驻视频流截屏替掉每次 235ms 的抓取；引擎线程池；int8 模板库；分辨率适配
- 快通道 250ms 扫亮度
